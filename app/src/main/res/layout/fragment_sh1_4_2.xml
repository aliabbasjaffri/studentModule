<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.coderize.monstertutors.RecordedFilesFragment">

    <LinearLayout
        android:layout_width="wrap_content"
        android:background="#12B8AC"
        android:layout_height="wrap_content"
        android:orientation="vertical">


        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="100dp"
            android:background="@color/tutor_content_background"
            android:text="NO REQUESTED CLASSES"
            android:textColor="@color/White"
            android:textSize="25sp"
            android:textStyle="bold"
            android:elevation="2dp"
            android:translationZ="2dp"
            android:id="@+id/no_list_text"
            android:visibility="gone"
            android:gravity="center"
            />

        <ListView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:id="@+id/requested_classes_list"
            android:visibility="gone"
            android:dividerHeight="3dp"
            ></ListView>

    </LinearLayout>
    <ProgressBar
        android:id="@+id/requested_classes_loading"
        style="?android:attr/progressBarStyleLarge"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="50dp"
        android:layout_gravity="center_vertical"
        />

</FrameLayout>
